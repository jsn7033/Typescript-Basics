const enum SeatChoice {
    AISLE = "aisle", //we can assign value which is optional
    MIDDLE = 10, //can be assigned numbers
    WINDOW = 16 //must be higher than previous number
}

const hcSeat = SeatChoice.AISLE;

